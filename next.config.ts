import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images:{
        domains: ["vn06itx8ks.ufs.sh","utfs.io","lh3.googleusercontent.com","picsum.photos"]
    }
};

export default nextConfig;
